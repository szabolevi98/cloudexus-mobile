<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; the window theme only sets the colour shown before the first frame. -->
    <style name="Theme.Cloudexus" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/cx_navy</item>
    </style>
</resources>
